services:
  - type: web
    name: agriguard-model
    env: python
    plan: free
    runtime: python
    pythonVersion: 3.10.13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
